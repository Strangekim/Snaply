/** Phase 3에서 Recorder 에이전트가 구현한다.
 * 역할: 녹화 소스 선택(setDisplayMediaRequestHandler), record:* IPC,
 * WebM → MP4/GIF 변환(ffmpeg-static), 트리밍 */
export function registerRecorderIpc(): void {
  // stub — Phase 3
}
